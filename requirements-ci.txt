# Lightweight requirements for CI runs on GitHub-hosted runners.
# - Uses a CPU-friendly PyTorch version to avoid GPU/CUDA wheels.
# - Keeps the rest aligned with requirements.txt.

# Data and ML dependencies
yfinance==0.2.65
numpy==1.26.4
requests==2.31.0
sentence-transformers==5.0.0
torch==2.4.1
forex-python==1.8
gdeltdoc

# Core dependencies
python-dotenv
pydantic
pydantic-extra-types
pycountry
chromadb
websockets
aiohttp
uvicorn
pillow
scikit-learn

# Langchain and related
langgraph>=0.5.2
langchain>=0.3.26
langchain_huggingface>=0.3.0
langchain_anthropic>=0.3.17
langchain_google_genai>=2.1.8
langchain_openai>=0.3.27
langchain_together>=0.3.0
langchain_core>=0.3.68
langchain_chroma

# Other frameworks (deployment, config, etc.)
fastapi==0.116.0
pyyaml>=6.0

# Test stack
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0
