[pytest]
markers =
    unit: fast unit tests
    integration: tests that hit the running FastAPI app
    e2e: end-to-end workflow simulations
    gpu: requires CUDA acceleration
    external: requires external API access
addopts = -q
